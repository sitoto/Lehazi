<h2><%= @article.title  %></h2>

  <% if @article.category  %>
  <p class="category">
    分类：<%= link_to @article.category.name, category_article_path(@article.category) %>
  </p>
  <% end %>
  <p>
    <%= @article.created_at.to_s(:short) %>  <br />
    <%=	 textilize(@article.body) %><br />
  </p>
<p><%=	 link_to '返回' , articles_url %></p>
